type StakingReward @entity {
  id: ID! #blockHieght-eventIdx
  account: String!
  balance: BigInt!
  date: Date!
  blockHeight: Int!
}

type SumReward @entity{
  id: ID! # AccountId
  totalReward: BigInt!
  blockheight: Int!
}